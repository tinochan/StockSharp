<Window x:Class="StockSharp.Samples.Chart.PnlCurve.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:charting="http://schemas.stocksharp.com/xaml"
		Title="PnL Curve Viewer" Height="600" Width="1000" Loaded="OnLoaded">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="280"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<DockPanel Grid.Column="0" Margin="5">
			<TextBlock DockPanel.Dock="Top" Text="Select securities (underlying + options):" TextWrapping="Wrap" Margin="0,0,0,5"/>
			<Button DockPanel.Dock="Top" x:Name="RefreshButton" Content="Reload" Click="OnRefreshClick" Margin="0,0,0,5"/>
			<TreeView x:Name="SecuritiesTree" ScrollViewer.VerticalScrollBarVisibility="Auto"/>
		</DockPanel>

		<charting:ChartPanel x:Name="Chart" Grid.Column="1" IsInteracted="True"/>
	</Grid>
</Window>
